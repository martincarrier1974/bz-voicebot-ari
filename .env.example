# ----- ARI (Asterisk) -----
ARI_URL=http://127.0.0.1:8088
ARI_USER=voicebot
ARI_PASS=CHANGE_ME
ARI_APP=voicebot
# Tone à la prise d'appel (laisser vide ou commenté = pas de tone). Ex: sound:beep, tone:ring;tonezone=fr
# WELCOME_TONE=

# ----- Media / RTP -----
# Local UDP port where YOUR media server listens for RTP from Asterisk ExternalMedia
RTP_LISTEN_IP=0.0.0.0
RTP_LISTEN_PORT=40000

# If your Asterisk is behind NAT / different interface, note it here
ASTERISK_PUBLIC_IP=127.0.0.1
# IP utilisée pour external_host (si vide, utilise ASTERISK_PUBLIC_IP)
# MEDIA_SERVER_IP=192.168.1.10

# Audio format used with ExternalMedia (ulaw typical for telephony, or slin16)
RTP_FORMAT=ulaw
SAMPLE_RATE=8000
TIMEZONE=America/Montreal

# ----- Microsoft 365 / Graph -----
M365_TENANT_ID=YOUR_TENANT_ID
M365_CLIENT_ID=YOUR_CLIENT_ID
M365_CLIENT_SECRET=YOUR_CLIENT_SECRET

# Multi-calendar routing (service -> mailbox)
CAL_SUPPORT=support@yourdomain.com
CAL_SALES=ventes@yourdomain.com
CAL_CABLING=cablage@yourdomain.com

# Business rules
APPT_DURATION_MIN=30
BUSINESS_HOURS_START=09:00
BUSINESS_HOURS_END=17:00
APPT_BUFFER_MIN=15

# ----- Deepgram (STT + TTS) -----
DEEPGRAM_API_KEY=
DG_STT_MODEL=nova-3
DG_TTS_MODEL=aura-2-agathe-fr
ELEVENLABS_API_KEY=

# ----- Agent conversationnel Deepgram (STT + LLM + TTS) -----
# Mettre USE_DEEPGRAM_AGENT=true pour utiliser l'agent au lieu de STT + règles + TTS
USE_DEEPGRAM_AGENT=false
# Modèle LLM (ex: gpt-4o-mini, gpt-5.2-instant). Clé OpenAI gérée par Deepgram si endpoint non fourni.
DG_AGENT_LLM_MODEL=gpt-4o-mini
DG_AGENT_GREETING=Bienvenue chez BZ Telecom, comment pouvons-nous vous aider aujourd'hui ?
# Prompt système (optionnel; un défaut BZ Telecom est utilisé si vide)
# DG_AGENT_PROMPT=
# Fichier runtime publié par l'admin et lu au début de chaque nouvel appel
RUNTIME_CONFIG_PATH=runtime/voicebot-config.json

# Destinations ARI pour transfert (optionnel; défaut Local/101@from-internal)
# TRANSFER_POSTE_101=Local/101@from-internal
# TRANSFER_POSTE_102=Local/102@from-internal
# TRANSFER_POSTE_105=Local/105@from-internal

# ----- Voice AI (legacy / optional) -----
VOICE_AI_API_KEY=
